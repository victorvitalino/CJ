.row
  .col-sm-12
    .m-b-md
      %h3.m-b-none 
        %span.pull-right 
          = link_to "artigos", administrator_posts_path, class: 'btn btn-info btn-sm'
          = link_to "nova categoria", new_administrator_post_category_path, class: 'btn btn-primary btn-sm'
        %b Categorias de Artigos
       
.row
  .col-sm-12
    %section.panel.panel-default
      %header.panel-heading
        %span.label.bg-success.pull-right.m-t-xs  
        Listagem
      %table.table.table-striped.m-b-none
        %thead
          %tr
            %th #
            %th Título
            %th Descrição
            %th Publicado?
            %th Ações
        %tbody
          - @post_categories.each_with_index do |category, index|
            %tr
              %td= index + 1
              %td= category.name
              %td= category.description
              %td= category.publish ? 'publicado' : 'não publicado'
              %td.col-md-2
                = link_to "editar", edit_administrator_post_category_path(category), class: 'btn btn-info btn-sm'
                = link_to "excluir", administrator_post_category_path(category), method: :delete, class: 'btn btn-danger btn-sm'

