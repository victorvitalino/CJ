.row
  .col-md-12
    %h2
      %b Artigos
%br/
%br/
%hr/
- @posts.order('created_at DESC').each do |post|
  .row
    .col-md-2
      = image_tag post.thumb_url, width: 80
    .col-md-10
      %h4
        %b= link_to post.title, portal_post_path(post)
      %p= post.resume.html_safe
      %hr/
      %p
        = post.author.name if post.author.present?
        = post.created_at.strftime('%d/%m/%Y às %H:%M')
  %hr/
