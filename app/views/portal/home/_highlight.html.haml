%br/
.well
  %div
    %h4
      %strong DESTAQUES
    - @posts.where(special: true).each do |post|
      .row
        .col-md-12
          .destaques
            %img.img-responsive.paddingbottom{:alt => "", :src => "#{post.thumb_url}"}/
            %h5
              %b= post.title
            %p 
              = post.resume
      %hr/